# Allow HTTP connections on port 22002 from the opsview server.
<% @monitoring_servers.each do |server| %>
-A INPUT -m state --state NEW -m tcp -p tcp --source <%= server[:ipaddress] %> --dport 22002 -j ACCEPT
<% end %>
